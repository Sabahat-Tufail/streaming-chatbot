# streaming-chatbot
A stateless, streaming AI chatbot built with FastAPI and Vanilla JavaScript, integrated with OpenRouter API for real-time, token-by-token responses. The backend streams responses, and the frontend renders them live with smooth UI and auto-scroll.
